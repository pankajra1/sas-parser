# Python dependencies for SAS Parser
# Using pyreadstat instead of JDBC for reading SAS files

# Core dependencies
pyreadstat>=1.2.0
pandas>=1.3.0
pyspark>=3.0.0

# Optional but recommended for better performance
numpy>=1.20.0

